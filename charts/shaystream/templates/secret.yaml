# Secret — נתונים רגישים (סיסמאות, JWT). ב-k8s זו הדרך הנכונה (במקום .env).
apiVersion: v1
kind: Secret
metadata:
  name: shaystream-secret
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.postgres.user | quote }}
  POSTGRES_PASSWORD: {{ .Values.postgres.password | quote }}
  POSTGRES_DB: {{ .Values.postgres.db | quote }}
  JWT_SECRET: {{ .Values.jwt.secret | quote }}
  JWT_EXPIRES_IN: {{ .Values.jwt.expiresIn | quote }}
  MINIO_ROOT_USER: {{ .Values.minio.rootUser | quote }}
  MINIO_ROOT_PASSWORD: {{ .Values.minio.rootPassword | quote }}
