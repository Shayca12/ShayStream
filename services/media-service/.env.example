# media-service — תבנית משתני סביבה (נכנס ל-Git).

DATABASE_URL="postgresql://shaystream:change-me@localhost:5434/shaystream_media?schema=public"

# חייב להיות זהה ל-JWT_SECRET של auth-service.
JWT_SECRET="change-me-to-a-long-random-secret"

PORT=3002

MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=shaystream
MINIO_SECRET_KEY=change-me-minio-password
MINIO_BUCKET=shaystream-media
